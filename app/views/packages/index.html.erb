<div class="page-header">
  <%= link_to new_package_path, class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-plus"></span>
    New Package
  <% end %>
  <h1>Listing packages</h1>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>

            <th></th>
            <th>Package #1</th>
            <th>Package #2</th>
            <th>Package #3</th>
      </tr>
    </thead>

    <tbody>
      <% @packages2.each_with_index do |row, index| %>
      <tr>
        <td><%= @row_headers[index] %></td>
      <% row.each do |value| %>
        <td><%= value %></td>
      <% end %>
      </tr>
    <% end %>

    </tbody>
  </table>

  <td><%= link_to 'Main page', root_path(@package), class: 'btn btn-info' %></td>
  <td><%= link_to 'Edit', edit_package_path(@packages), class: 'btn btn-info' %></td>

  <!--<table>-->
    <!--<% @packages2.each do |row| %>-->
      <!--<tr>-->

    <!--<% end %>-->
    <!--</tr>-->
  <!--</table>-->
</div>
